# Shared experiment configuration.
# Values marked null or [] must be frozen before a formal run.
experiment_name: calibration-collapse
case_count: null
case_ids: []

dataset:
  name: agentclinic-medqa

backbones: []

conditions:
  - sequential
  - randomized_order
  - static

turns:
  dialogue: null
  static: 1
  bins:
    early_max_fraction: 0.3333333333333333
    middle_max_fraction: 0.6666666666666666

seeds:
  - 0

confidence:
  minimum: 0
  maximum: 100

output:
  directory: results/raw_outputs
  turn_file: turns.jsonl
  schema_version: 1
